/**
 * No API routes exist for this static site.
 * This file intentionally exists to satisfy the "hooks" generation step,
 * and to provide a safe place for future data hooks.
 */
export function useNoop() {
  return null;
}
