## Packages
(none needed)

## Notes
Static marketing website: no API calls; hooks folder intentionally minimal.
Uses existing shadcn/ui components already in repo.
Uses framer-motion (already installed) for section reveals + ambient motion.
Uses react-hook-form + @hookform/resolvers + zod (already installed) for contact form validation.
Theme: custom ThemeProvider toggles `dark` class on documentElement and persists to localStorage.
